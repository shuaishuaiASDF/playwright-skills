{
  "name": "ui-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "test": "playwright test -c playwright.config.ts",
    "test:headed": "playwright test -c playwright.config.ts --headed",
    "test:list": "playwright test -c playwright.config.ts --list"
  },
  "devDependencies": {
    "@playwright/test": "^1.60.0",
    "@types/node": "^25.9.3",
    "typescript": "^6.0.3"
  }
}
