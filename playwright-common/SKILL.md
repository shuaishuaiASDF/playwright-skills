---
name: "playwright-common"
description: "Playwright UI/E2E test analysis, scenario matrix design, spec implementation, reusable test capability extraction, network/data waiting patterns, reporter integration, and audit-report generation inside an existing Playwright or ui-test framework. Use when Codex is asked to analyze a page for automation, implement or review Playwright specs, design coverage matrices, wire project reporter evidence, or produce audit-ready validation reports for a repo that already has its Playwright base scaffold. Do not use for first-time Playwright or ui-test initialization, base-directory scaffolding, package or tsconfig or playwright config creation, reporter or DB template bootstrapping, or other bootstrap work; in those cases stop the current flow and ask the user to switch to playwright-bootstrap explicitly."
---

# Playwright Common

## 快速执行摘要

- 先读代码，再分析；除非用户明确说“直接落地”，否则不要一开始就写脚本。
- 结论必须基于真实代码、真实接口、真实运行结果，禁止猜测页面能力、接口行为、校验规则。
- 优先复用项目内已有 Playwright 公共能力、测试夹具、登录态方案、报告模板、选择器约定；新增能力只做最小必要沉淀。
- 如果需求是“首次初始化 Playwright 基础框架 / 搭建 ui-test 初始目录 / 补齐基础骨架”，不要在本 Skill 内处理，请先调用 `$playwright-bootstrap` 这个初始化 skill。
- 当检查到仓库缺少基础骨架时，本 Skill 只负责识别边界并停止当前流程，不在此展开初始化步骤、模板复制或依赖安装。
- 报告必须优先对齐当前 Skill 定义的标准审计结构；若仓库内已有成熟自动化脚本或报告实现，则实现方式应与该标准保持一致。
- 复杂字段回归优先采用字段审计模型，为每个字段定义来源、校验方式与比对规则，不要在 spec 里零散堆积断言。
- `PASS / WARN / FAIL / SKIP` 要严格区分：业务断言不成立时应记录审计 `FAIL`，依赖缺失时记录 `SKIP`，不要把可审计的业务结果包装成脚本崩溃。

## 作用

该 Skill 用于为任意项目生成或分析 Playwright 自动化测试，覆盖以下两类任务：

- 页面自动化测试分析：先读真实代码与真实运行结果，再输出能力清单、场景矩阵、风险点、公共能力建议。
- 页面自动化测试落地：在分析基础上实现 Playwright 脚本、复用公共能力、跑验证，并生成统一报告。

## 与项目 Skill 的关系

- 本 Skill 是 Playwright 自动化的标准层与母版规范。
- 本 Skill 不负责新项目的 Playwright 基础框架初始化。
- 若仓库内同时存在“项目专用 Playwright Skill”，则项目专用 Skill 应继承本 Skill 的标准，再补充项目差异，不应重复复制整份通用规则。
- 当任务明确命中某个项目专用 Skill 的触发条件时，应优先触发项目专用 Skill；本 Skill 继续作为其底层标准。
- 当任务不属于任何项目专用 Skill，或当前仓库没有项目专用 Skill 时，直接使用本 Skill。
- 项目专用 Skill 只应覆盖以下内容：
  - 项目目录约定
  - 公共能力位置
  - 登录态与环境基建
  - reporter 接入方式
  - 文件编码、换行与命名规则
  - 少量项目特有的审计口径

## 用户最小输入

通常只需要以下信息即可启动：

- 路由或页面入口
- 角色或测试身份
- 核心场景
- 执行方式：先分析 / 直接落地

推荐输入格式：

```md
请按 playwright-common Skill 执行。
- 路由：/business-object
- 角色：[测试角色]
- 核心场景：业务对象的新增、编辑、删除、查询
- 执行方式：先分析 / 直接落地并跑验证
```

## 推荐产物

- 分析输出模板：`templates/analysis-output-template.md`
- 场景矩阵模板：`templates/scenario-matrix-template.md`
- 聚合审计报告模板：`templates/audit-report-template.md`
- 项目报告接入示例：`examples/audit-reporter-integration.md`
- 字段审计最小示例：`examples/field-audit-ui-api-pattern.md`
- 证据与报告规则：`references/evidence-and-reporting.md`
- 网络与测试数据模式：`references/network-and-data-patterns.md`
- 落地实现检查清单：`references/implementation-checklist.md`
- README 文档模板：`templates/readme-template.md`

## 边界规则

- 若当前仓库未发现 `playwright.config.ts`，或 `package.json` 中未声明 `@playwright/test` 依赖，则判定为缺少可用的 Playwright 基础骨架；应停止当前流程，并明确提示：请先调用 `$playwright-bootstrap` 这个初始化 skill。
- 若任务目标是首次创建或首次补齐 `ui-test/` 基础骨架，或需要首次创建 `package.json`、`tsconfig.json`、`playwright.config.ts`、`.env.db.local`、初始化 README、reporter/DB 模板等基础文件，应停止当前流程，并明确提示：请先调用 `$playwright-bootstrap` 这个初始化 skill。
- 本 Skill 只负责已有框架内的分析、实现、评审、接入与验证，不展开初始化方案、模板复制或依赖安装。

## 执行总原则

1. 所有结论必须基于真实代码、真实接口、真实运行结果，禁止猜测页面能力、接口行为、校验规则。
2. 默认先分析，只有当用户明确说“直接落地”时，才直接写脚本并跑验证。
3. 优先复用项目已有的 Playwright 公共能力、fixture、page object、test util、mock 工具、reporter 与认证方案，不要重复造轮子。
4. 修改范围遵循最小原则，只改与当前自动化测试直接相关的文件。
5. 若项目内已有成熟报告模板、目录约定、测试命名规范、公共脚手架，应优先完全对齐，不要自行发明新的结构。
6. 写脚本前先确认该项目的登录态、环境切换、测试数据准备、截图与报告输出方式；若缺失，先在分析结论中指出，不要编造。
7. 涉及 Playwright 相关脚本、配置、报告模板、辅助工具文件时，必须保持原项目既有编码与换行格式；新增文件默认跟随项目约定，修改已有文件时禁止擅自变更编码和换行。
8. 写入或批量改动 Playwright 相关文件后，必须自检编码、换行、导入与诊断结果，确保没有因为工具写入破坏项目格式。

## 标准执行流程

推荐按以下顺序执行，避免跳步骤导致结论失真：

1. 识别页面入口、角色、环境、核心场景与执行方式。
2. 先检查仓库内是否存在 `playwright.config.ts`，以及 `package.json` 是否声明 `@playwright/test`；若任一项不满足，停止当前流程，并提示用户先调用 `$playwright-bootstrap` 这个初始化 skill。
3. 阅读最小必要代码与已有测试资产，确认页面真实能力、接口入口、公共基建和报告约定。
4. 判断是否存在待确认项，例如登录态不明、接口不明、数据前置条件不明、是否允许 mock 不明。
5. 若是分析模式，优先使用 `templates/analysis-output-template.md` 与 `templates/scenario-matrix-template.md` 输出分析结果。
6. 若是直接落地模式，先完成最小分析，再开始实现脚本、补充必要公共能力、运行验证。
7. 执行后使用 `templates/audit-report-template.md` 或项目既有报告模板生成结果总结。
8. 交付时必须明确说明复用了哪些项目资产、参考了哪些已有脚本，以及本次是否完全对齐其结构。

## 模板使用要求

- 当前 Skill 目录下的模板就是标准模板：
  - 分析阶段按 `templates/analysis-output-template.md` 输出
  - 场景设计按 `templates/scenario-matrix-template.md` 输出
  - 验证完成后按 `templates/audit-report-template.md` 输出
- README 文档按 `templates/readme-template.md` 维护
- 若用户只要求口头分析，可不强制生成模板文件，但输出结构仍应尽量对齐模板。
- 若用户要求"直接落地并跑验证"，分析、实现、验证、报告四段内容仍应完整，只是可以在一次执行中连续完成。
- 如果项目已有 reporter 或 markdown 生成器，应优先让该工具按本 Skill 的标准模板结构产出，而不是另起一套格式。
### Reference 读取策略

- 需要判断审计状态、报告证据、API/DB/文件等外部原证时，读取 `references/evidence-and-reporting.md`。
- 需要处理响应监听、首屏自动请求误匹配、真实数据与 mock 分层、测试数据生命周期时，读取 `references/network-and-data-patterns.md`。
- 准备落地或评审一个可维护 Playwright 脚本时，读取 `references/implementation-checklist.md` 做实现前后检查。
- Reference 里的规则必须保持公共化；项目路径、接口名、字段名、账号、业务文案应沉淀到项目专用 Skill 或项目规则中。

### README 文档维护要求

- 每次新增测试模块、公共能力或测试脚本时，必须同步更新项目测试 README 或脚本清单。
- README 结构参考 `templates/readme-template.md`，但是否插入项目级标记仍由目标仓库规则决定。
- 无论是“先分析”还是“直接输出/直接落地”，`测试场景矩阵` 都必须使用 Markdown 表格展示，不要改成散点列表。
- 场景矩阵表格至少应包含以下列：
  - `场景编号`
  - `场景名称`
  - `类型`
  - `前置条件`
  - `操作步骤`
  - `核心断言`
  - `覆盖状态`
- `场景编号` 必须使用中文类型前缀 + 两位数字的顺序编号，默认格式为 `正常流01 / 边界流01 / 异常流01`；若确有联调场景，则使用 `联调场景01`。
- 同一类型内按矩阵出现顺序递增编号，不要再使用 `N01 / B01 / E01` 这类英文缩写编号。
- `覆盖状态` 至少使用以下口径之一：
  - `已覆盖`
  - `部分覆盖`
  - `未覆盖`
  - `规划中`
- 如果本轮只实现了部分场景，必须在同一张矩阵表里同步标记已覆盖与未覆盖范围，不能只列计划不写当前进度。
- 测试场景矩阵必须一次性列出当前已经识别到的全部场景，不管本轮实际只写了多少，都不能分几次逐步补列。
- 若后续发现新增场景，应说明这是“新识别出的场景”，并回填到完整矩阵，而不是仅追加一个零散补充列表。

## 报告与证据

- 报告优先对齐 `templates/audit-report-template.md`；若项目已有 reporter、markdown 生成器或统一报告基建，应优先复用并保持结构一致。
- 报告器代码层的 `section` 入参统一使用 `normal / boundary / exception`；确有联调章节时使用 `integration`。中文章节名只作为展示名，不要直接传入 `logWithSection`。
- 若项目已有 reporter 但当前页面尚未接入，可参考 `examples/audit-reporter-integration.md` 设计最小接入方案。
- `PASS / WARN / FAIL / SKIP` 语义、外部原证要求、报告聚合方式与失败态生成规则，统一读取 `references/evidence-and-reporting.md`，不要在实现时自行发明另一套口径。

## 必做阅读范围

按执行方式分档阅读，避免轻任务前置成本过高：

- 分析模式（最小阅读集）：
  - 页面主文件
  - API 文件或数据请求入口
  - 同模块或同风格的已有测试脚本至少 1 个
- 落地模式（完整阅读集）：
  - 页面主文件
  - 关键组件文件
  - API 文件或数据请求入口
  - 项目已有 Playwright 公共能力 / 工具 / fixture
  - 同模块或同风格的已有测试脚本
  - 现有报告实现方式

## 分析阶段输出要求

如果用户要求先分析，则必须输出以下内容后再等待确认：

1. 页面能力清单
- 标明主文件、组件文件、API 文件
- 按“功能点 -> 代码来源”输出

2. 测试场景矩阵
- 场景编号：使用 `正常流01 / 边界流01 / 异常流01` 这类中文顺序编号
- 场景名称
- 类型：默认按 正常 / 边界 / 异常 归类；仅在确有跨接口或跨页面串联校验需求时，才追加 联调场景
- 前置条件
- 操作步骤
- 核心断言
- 输出矩阵时，场景名称与场景编号要能一一对应，方便后续在报告、脚本和问题追踪里直接引用。
- 覆盖状态：至少标明 `已覆盖 / 部分覆盖 / 未覆盖 / 规划中`
- 输出形式必须直接使用表格，不要把以上字段拆成多段列表描述。
- 矩阵内容必须是“当前已识别的全量场景集合”，不要只输出本轮实现到的子集。

3. 高风险交互点清单
- 风险点
- 风险原因
- 自动化应对策略

4. 公共能力设计
- 已复用能力
- 建议新增沉淀能力
- 每个能力解决什么问题

5. 已知脆弱点与改进建议

## 落地阶段实现要求

如果用户明确要求“直接落地”，则在分析基础上继续执行实现与验证。

### 场景覆盖

至少覆盖：

- 正常流 ≥ 1
- 边界流 ≥ 2
- 异常流 ≥ 3

需要重点覆盖：

- 必填项逐项校验
- 单字段查询
- 联合查询或重置清空
- 下拉每种选项验证
- 新增 / 编辑 / 删除 / 查询等核心链路

- 轻量页面允许豁免为“异常流 ≥ 2”，但必须在报告摘要中写明：
  - 页面为何判定为轻量页面
  - 本次豁免原因与替代覆盖策略
- 默认可参考以下条件判定为轻量页面：仅单列表或单表单、无新增/编辑/删除主链路、无复杂联动、无异步状态流转。

### 流类型通用归类规则

- 边界流用于验证“规则边界是否正确生效”，例如长度、字符、数值、状态切换临界点。
- 异常流用于验证“失败处理是否可控”，例如报错提示、按钮解锁、状态回滚、可重试性。
- 正常流用于验证“核心业务链路是否跑通”，例如从入口操作到结果回显的主路径闭环。
- 同一场景只允许归类一次，不要同时计入边界流与异常流；归类以“主验证目的”优先。
- 参数合法性边界归入边界流；参数触发失败后的恢复与处置归入异常流。


### 联调场景使用规则

- `联调场景` 不是默认主分类，默认仍以 `正常流 / 边界流 / 异常流` 三类为主。
- 只有在单一页面或单一接口无法证明业务闭环时，才允许追加 `联调场景`，例如：
  - 需要跨接口串联验证前后依赖关系
  - 需要跨页面或跨模块验证数据流转结果
  - 需要验证“上游动作成功后，下游查询/回显/状态变更是否成立”
- 若只是普通成功路径、参数校验或失败处置，不要额外创建 `联调场景`，应分别归入 `正常流 / 边界流 / 异常流`。
- 若追加了 `联调场景`，输出中必须说明：
  - 为什么三类默认分类不足以覆盖当前验证目标
  - 串联了哪些接口、页面或模块
  - 联调结论依赖哪些关键断言或证据
- `联调场景` 只能作为补充章节，不能替代默认三类，也不能把所有主路径都泛化为“联调”。


### 定位与交互要求

- 定位器必须优先使用业务语义或可访问性语义。
- 优先使用稳定的 `getByRole`、`getByLabel`、`getByText`、`data-testid`、业务主键或页面对象封装。
- 禁止使用 `nth-child`。
- 禁止使用纯索引定位。
- 禁止使用脆弱的 class 链作为主定位方案。
- 若新增控件操作逻辑，优先沉淀为最小必要的公共能力。

### 断言要求

断言必须校验业务结果，而不只是元素存在。至少包括：

- 接口是否成功
- 页面提示文案是否正确
- 列表 / 表单 / 弹窗状态是否符合预期
- 新增 / 编辑 / 删除 / 查询结果是否真实生效
- 关键字段是否与真实返回或持久化结果一致

### 字段一致性校验要求

- 对于“查询命中后字段是否一致”这类校验，不要默认理解成“整行 UI 文本全量比对”。
- 先判断字段属于哪类：
  - `UI稳定展示字段`：走页面可见值校验
  - `UI不稳定展示字段` 或 `当前页未展示字段`：走接口 payload 校验
- 不要把“页面当前未展示该字段”直接判成脚本失败；要先判断该字段是否本就不属于当前页面稳定展示范围。
- 唯一键查询命中后，建议至少补一层“关键字段一致性校验”；但这层一致性校验必须基于真实展示情况决定校验来源，不能把所有字段都强压到 UI 上。
- 列表页字段一致性校验若采用 `UI + API` 混合方式，报告中必须明确标注字段来源，例如：
  - `用户名(UI回显)`
  - `手机号(API回查)`
  - `状态(UI回显)`
- 字段审计模型、最小字段集与高频模式示例统一参考 `examples/field-audit-ui-api-pattern.md`，不要在 spec 中零散堆积 `toContainText`。

## 查询场景专项规则

- 查询默认只允许单次执行；仅在新增后立即查询、编辑后立即查询等明确存在最终一致性场景时，才允许最小范围短轮询。
- 查询断言优先看接口总条数、筛选是否真实生效、目标数据是否命中，再决定是否继续做字段审计。
- 查询为 0 条时，不要直接抛技术异常；验证空态可 `PASS`，期望命中应 `FAIL`，样本缺失或依赖未配置应 `SKIP` 或保持部分覆盖。
- 初始化自动请求误匹配、请求上下文过滤、真实数据与 mock 分层、轮询边界和零命中口径，统一读取 `references/network-and-data-patterns.md`。

## 等待策略

- 单次显式等待尽量不超过 5 秒。
- 优先等待明确事件，例如接口响应、提示出现、弹窗关闭、表格刷新、URL 变化、加载骨架消失。
- 不要大量使用固定 sleep。
- 若页面存在初始化自动请求、自动重置、分页刷新、表格重绘竞态，要先识别再决定等待点。

## 登录态与环境策略

- 优先复用项目已有的 `storageState`、session 注入、cookie、token fixture 或登录助手。
- 避免进入页面后二次登录，除非项目确实没有稳定的复用方案。
- 若被重定向到登录页、权限页或环境拦截页，要快速失败并明确提示原因。
- 涉及多环境、租户、语言、时区、feature flag 时，先识别再落地，不要默认假设只有单一运行环境。

## 异常场景要求

- 未指定时，异常流默认优先级为：
  - 接口 `5xx` 失败处置
  - 接口超时后的重试闭环
  - 重复提交的幂等处置
- 若页面不适配默认优先级，可替换异常项，但必须在报告中写明替换项与原因。
- 异常场景必须明确说明“如何触发异常”。
- 优先使用可控方式，例如 `route mock`、测试桩、可回滚测试数据或受控环境开关。
- 若使用 `mock`，必须明确写出：
  - mock 的接口
  - mock 的返回值
  - 预期页面表现

## 真实数据与 Mock 分层

- 真实数据优先覆盖登录、权限、主链路、持久化、跨页面数据流和外部状态验证。
- Mock 或 route-controlled 数据优先覆盖无数据、前端分支、异常响应、超时、取消、罕见状态组合。
- 同一个页面或业务流程的真实主链路与 mock 分支可以进入同一个 spec 和同一份审计报告，但必须在场景名称或证据里说明数据来源。
- 不要为了追求真实数据覆盖而强行依赖不可控样本；不可控样本会导致覆盖状态失真。

## 输出要求

### 分析模式输出

若当前任务是“先分析”，本轮输出至少包括：

1. 页面能力清单
2. 测试场景矩阵
3. 高风险交互点清单
4. 通用公共组件或能力设计
5. 已知脆弱点与改进建议
其中第 2 项 `测试场景矩阵` 必须直接输出为表格，并明确每条场景当前处于 `已覆盖 / 部分覆盖 / 未覆盖 / 规划中` 的哪一种状态。

### 落地模式最终输出

若当前任务是“直接落地”或“分析确认后继续实现”，最终输出至少包括：

1. 页面能力清单
2. 测试场景矩阵
3. 高风险交互点清单
4. 通用公共组件或能力设计
5. Playwright 落地代码
6. 验证结果
7. 已知脆弱点与改进建议
其中 `测试场景矩阵` 仍必须保持表格形式，并在同一张表中同步标记本轮已覆盖与未覆盖范围，不能因为已经开始实现就省略矩阵或改成自然语言总结。

如项目没有现成结构，推荐分别参考：

- 分析阶段：`templates/analysis-output-template.md`
- 场景设计：`templates/scenario-matrix-template.md`
- 项目报告或兜底报告：`templates/audit-report-template.md`

## 使用提醒

- 如果用户说“先不要直接写脚本”，必须先完成分析输出，再等确认。
- 如果用户说“直接落地”，可以直接实现并跑验证。
- 如果参考了仓库内已有同类脚本的报告格式或实现方式，必须明确说明参考了哪个脚本，以及本次是否保持一致。
- 若仓库内已有同类页面的成熟报告模板或聚合报告实现，优先完全对齐其结构，不要自行发明新的报告格式。
